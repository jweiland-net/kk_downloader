# This config part can be removing when TYPO3 9 compatibility was removed
config.tx_extbase {
  persistence.classes {
    JWeiland\KkDownloader\Domain\Model\Download {
      mapping {
        tableName = tx_kkdownloader_images
        columns {
          name.mapOnProperty = title
          image.mapOnProperty = files
          imagepreview.mapOnProperty = preview
          longdescription.mapOnProperty = longDescription
          downloaddescription.mapOnProperty = filesDescription
          categories.mapOnProperty = cat
        }
      }
    }
    JWeiland\KkDownloader\Domain\Model\Category {
      mapping {
        tableName = tx_kkdownloader_cat
        columns {
          cat.mapOnProperty = title
        }
      }
    }
  }
}
